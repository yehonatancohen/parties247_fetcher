version: "3.9"

services:
  goout-scraper:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    shm_size: "256mb"
    volumes:
      - playwright_data:/root/.cache/ms-playwright

volumes:
  playwright_data:
